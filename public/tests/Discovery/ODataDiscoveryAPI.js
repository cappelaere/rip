var chai	= require('chai');
var AssertionError	= chai.AssertionError;

describe('Discovery Service', function(){
	describe('Using OData Discovery Document', function() {
		
		describe('is accessible', function() {
			it('should exists')
		})
		
		describe('contains API metadata', function() {
			it('should contain service metadata')
			it('should contain resource collections')
			it('should contain schemas')
			it('should contain security protocol')
		})
	})
});