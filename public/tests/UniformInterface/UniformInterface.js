describe('Uniform_Interface', function(){
	describe('uses limited http verbs', function() {
		it('should use GET|PUT|POST|HEAD|DELETE|OPTIONS', function(){
		})
	})
	describe('follows idempotence rules', function() {
		it('should use idempotent GET', function(){
		})
		it('should use idempotent PUT', function(){
		})
		it('should use idempotent DELETE', function(){
		})
		it('should use non-idempotent POST', function(){
		})
	})
})
